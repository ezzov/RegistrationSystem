package com.example.registrationsystem.models.enums;

import com.example.registrationsystem.models.Request;

/**
 * Enum is used in {@link Request}
 */
public enum Status {
    DRAFT,
    SENT,
    ACCEPTED,
    REJECTED;
}
